<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.groupware_mini.groupware.mapper.AccountMapper">

    <select id="findByEmployeeName" parameterType="string" resultType="com.groupware_mini.groupware.dto.EmployeeDto">
        SELECT * FROM employee_info WHERE employee_name = #{employeeName}
    </select>

</mapper>